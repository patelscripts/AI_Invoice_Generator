version: "3"

services:
  backend:
    build: ./Backend   
    ports:
      - "8000:8000"
    env_file:
     - ./Backend/.env   

  frontend:
    build: ./Frontend  
    ports:
      - "5173:5173"
    depends_on:
      - backend